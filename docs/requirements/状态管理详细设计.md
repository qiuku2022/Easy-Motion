# 状态管理详细设计

## 概述

本文档定义渲染进程（Renderer）内 Zustand Store 的完整接口、状态流转规则、跨 Store 通信机制和持久化策略。所有 Store 定义与 `技术规格.md` 中"状态管理"章节、`应用架构详细设计.md` 中目录结构对齐。

> **版本**：v0.1  
> **最后更新**：2026-05-30  
> **关联文档**：技术规格.md、时间线编辑.md、交互流程.md、应用架构详细设计.md

---

## 架构原则

### 1. Store 职责单一
每个 Store 只管理一类状态，避免 God Object。Store 之间通过订阅/广播或 Action 委托通信，不直接互相调用 `getState()`。

### 2. 不可变更新
所有状态更新通过 Immer 处理，确保时间旅行调试和 React 重渲染优化。

### 3. 选择性持久化
- **自动持久化**：projectStore、timelineStore（写入 `project.json` / `subproject.json`）
- **会话级**：uiStore、previewStore（重启后重置）
- **持久化+隔离**：conversationStore（每个子项目独立的 `conversation.json`）

### 4. 撤销栈独立
timelineStore 维护独立的撤销/重做栈，其他 Store 的变更不参与撤销（避免误操作）。

---

## Store 清单与接口定义

### 1. projectStore

**职责**：总项目与子项目的生命周期管理、当前激活上下文。

```typescript
// stores/projectStore.ts
interface ProjectState {
  // ── 项目列表 ──
  projects: ProjectSummary[];          // 最近打开的项目列表（来自 library.db）
  currentProjectId: string | null;   // 当前激活的总项目 UUID
  currentSubprojectId: string | null; // 当前激活的子项目 UUID

  // ── 加载状态 ──
  isLoading: boolean;                // 项目列表加载中
  isSaving: boolean;                 // 正在保存（防抖中显示指示器）
  lastSavedAt: number | null;         // 上次成功保存时间戳

  // ── 错误状态 ──
  error: ProjectError | null;
}

interface ProjectSummary {
  id: string;
  name: string;
  path: string;
  thumbnailPath?: string;
  resolution: string;     // 如 "1920x1080"
  fps: number;
  subprojectCount: number;
  modifiedAt: number;
  lastOpenedAt?: number;
}

interface ProjectError {
  code: string;           // 如 'E2015'
  message: string;
  action?: 'retry' | 'ignore' | 'abort';
}

interface ProjectActions {
  // 项目级操作
  loadProjects(): Promise<void>;                         // 从 library.db 加载项目列表
  createProject(config: ProjectConfig): Promise<string>; // 返回新项目 UUID
  openProject(projectId: string): Promise<void>;
  deleteProject(projectId: string): Promise<void>;
  renameProject(projectId: string, name: string): Promise<void>;

  // 子项目级操作
  createSubproject(name: string, template?: string): Promise<string>;
  switchSubproject(subprojectId: string): Promise<void>;
  renameSubproject(subprojectId: string, name: string): Promise<void>;
  deleteSubproject(subprojectId: string): Promise<void>;
  duplicateSubproject(subprojectId: string): Promise<string>;

  // 状态更新
  setCurrentProject(projectId: string | null): void;
  setCurrentSubproject(subprojectId: string | null): void;
  setError(error: ProjectError | null): void;
  markSaved(): void;
}
```

**状态流转图：**

```
          ┌─────────────┐
          │   initial   │
          │ (projects=[],
          │  current=null)
          └──────┬──────┘
                 │ loadProjects()
                 ▼
          ┌─────────────┐     createProject()
          │   loaded    │◄──────────────────┐
          │ (列表就绪)   │                     │
          └──────┬──────┘                     │
                 │ openProject()             │
                 ▼                           │
          ┌─────────────┐     deleteProject()
          │  project    │───────────────────┐
          │  opened     │                     │
          │ (current set)                     │
          └──────┬──────┘                     │
                 │ switchSubproject()          │
                 ▼                             │
          ┌─────────────┐                     │
          │ subproject  │─────────────────────┘
          │  active     │   (回到 loaded，
          └─────────────┘    保留项目列表)
```

**跨 Store 影响：**
- `openProject()` → 触发 `timelineStore.loadTimeline()`
- `switchSubproject()` → 触发 `timelineStore.loadTimeline()` + `conversationStore.loadConversation()`
- `createSubproject()` → 成功后自动 `switchSubproject()`

---

### 2. timelineStore

**职责**：时间线数据（轨道、片段、关键帧）、播放控制、历史栈。

```typescript
// stores/timelineStore.ts
interface TimelineState {
  // ── 时间线配置 ──
  timeline: Timeline | null;        // 见 时间线编辑.md
  fps: number;
  durationInFrames: number;
  width: number;
  height: number;

  // ── 播放控制 ──
  currentFrame: number;              // 当前播放头位置
  isPlaying: boolean;
  playbackSpeed: number;             // 0.25, 0.5, 1, 2, 4
  loopRange: [number, number] | null; // 循环播放区间

  // ── 选中状态 ──
  selectedTrackId: string | null;
  selectedClipId: string | null;
  selectedKeyframeId: string | null;

  // ── 历史栈（撤销/重做）──
  history: {
    past: Timeline[];                // 最大 50 步
    future: Timeline[];
  };

  // ── 状态标志 ──
  hasUnsavedChanges: boolean;          // 自上次保存后是否有变更
  isGenerating: boolean;             // AI 正在生成代码
  generatorProgress: number;         // 0-100
}

interface TimelineActions {
  // 时间线加载/保存
  loadTimeline(subprojectId: string): Promise<void>;
  saveTimeline(): Promise<void>;    // 写入 subproject.json，触发 projectStore.markSaved()

  // 轨道操作
  addTrack(type: TrackType, name?: string): void;
  removeTrack(trackId: string): void;
  reorderTracks(trackIds: string[]): void;
  toggleTrackVisibility(trackId: string): void;
  toggleTrackLock(trackId: string): void;

  // 片段操作
  addClip(trackId: string, clip: Clip): void;
  removeClip(trackId: string, clipId: string): void;
  moveClip(clipId: string, targetTrackId: string, newStartFrame: number): void;
  resizeClip(clipId: string, newDuration: number): void;
  splitClip(clipId: string, splitFrame: number): void; // 在指定帧分割

  // 关键帧操作
  addKeyframe(clipId: string, property: string, keyframe: Keyframe): void;
  removeKeyframe(clipId: string, keyframeId: string): void;
  updateKeyframe(clipId: string, keyframeId: string, updates: Partial<Keyframe>): void;

  // 选中控制
  selectTrack(trackId: string | null): void;
  selectClip(clipId: string | null): void;
  selectKeyframe(keyframeId: string | null): void;

  // 播放控制
  play(): void;
  pause(): void;
  seekTo(frame: number): void;
  setPlaybackSpeed(speed: number): void;
  setLoopRange(range: [number, number] | null): void;
  stepForward(frames?: number): void;  // 默认 1 帧
  stepBackward(frames?: number): void;

  // 历史操作
  undo(): void;
  redo(): void;
  canUndo(): boolean;
  canRedo(): boolean;
  clearHistory(): void;

  // AI 生成
  setGenerating(isGenerating: boolean, progress?: number): void;
}
```

**历史栈（撤销/重做）规则：**

| 操作类型 | 是否入栈 | 说明 |
|----------|----------|------|
| 添加/删除轨道 | ✅ | 完整 Timeline 快照 |
| 添加/删除/移动片段 | ✅ | 完整 Timeline 快照 |
| 修改关键帧 | ✅ | 但 500ms 防抖合并连续操作 |
| 播放控制（seek/play） | ❌ | 仅 UI 状态，不入栈 |
| 选中变更 | ❌ | 仅 UI 状态，不入栈 |
| AI 生成完成 | ✅ | 生成前后的完整快照 |

**状态流转图：**

```
         ┌──────────────┐
         │   initial    │
         │ (timeline=null)
         └──────┬───────┘
                │ loadTimeline()
                ▼
         ┌──────────────┐
         │    loaded    │◄──────────────┐
         │ (可操作)      │               │
         └──────┬───────┘               │
                │                       │
    ┌───────────┼───────────┐           │
    ▼           ▼           ▼           │
┌───────┐  ┌───────┐  ┌───────┐        │
│  add  │  │ move  │  │ delete│        │
│ track │  │ clip  │  │ keyframe      │
└───┬───┘  └───┬───┘  └───┬───┘        │
    │          │          │             │
    └──────────┴──────────┘             │
               ▼                        │
        ┌──────────────┐                │
        │ push history │                │
        │ set hasUnsavedChanges=true    │
        └──────┬───────┘                │
               │ undo()                 │
               ▼                        │
        ┌──────────────┐                │
        │   restore    │────────────────┘
        │  from past[] │   (redo() → restore from future[])
        └──────────────┘
```

**跨 Store 影响：**
- `addClip()` 等变更操作 → `hasUnsavedChanges = true`
- `saveTimeline()` → 调用 IPC `main:saveSubproject` → 成功后 `hasUnsavedChanges = false` → `projectStore.markSaved()`
- `play()` / `seekTo()` → 通知 `previewStore` 同步 iframe
- `selectClip()` → 通知 `uiStore` 更新属性面板显示

---

### 3. conversationStore

**职责**：对话历史、Agent 任务状态、流式消息缓冲。

```typescript
// stores/conversationStore.ts
interface ConversationState {
  // ── 对话数据 ──
  conversation: Conversation | null;  // 见 交互流程.md
  messages: Message[];                 // 当前对话消息列表（展开后的数组）

  // ── Agent 状态 ──
  agentStatus: 'idle' | 'parsing' | 'generating' | 'reviewing' | 'error';
  currentTask: AgentTask | null;      // 当前执行中的任务
  taskHistory: AgentTask[];           // 已完成的任务（最近 20 个）

  // ── 流式输出缓冲 ──
  streamingMessageId: string | null;   // 正在流式输出的消息 ID
  streamingBuffer: string;            // 未完成的流式文本缓冲

  // ── 输入状态 ──
  inputText: string;                  // 对话面板输入框当前文本
  attachedImages: AttachedImage[];   // 当前附带的参考图片
  isInputDisabled: boolean;          // Agent 生成中时禁用输入
}

interface AgentTask {
  id: string;
  type: 'generate' | 'modify' | 'explain' | 'crawl';
  status: 'pending' | 'running' | 'completed' | 'failed';
  startedAt: number;
  completedAt?: number;
  params: Record<string, unknown>;   // 任务参数（如用户输入、图片引用）
  result?: Record<string, unknown>; // 任务结果摘要
  error?: string;
}

interface Message {
  id: string;
  role: 'user' | 'assistant' | 'system';
  content: string;
  timestamp: number;
  attachedImages?: string[];         // 图片引用 ID 列表
  codeDiff?: CodeDiff;               // 代码变更（Assistant 消息）
  actionButtons?: ActionButton[];    // 快捷操作按钮（如"应用修改"）
}

interface ConversationActions {
  // 对话生命周期
  loadConversation(subprojectId: string): Promise<void>;
  saveConversation(): Promise<void>;

  // 消息操作
  sendMessage(text: string, images?: AttachedImage[]): Promise<void>;
  appendStreamingChunk(chunk: string): void;  // 流式输出追加
  finalizeStreamingMessage(): void;           // 流式输出结束
  cancelGeneration(): void;                   // 用户点击取消

  // 图片管理
  attachImage(image: AttachedImage): void;
  removeImage(imageId: string): void;
  reorderImages(imageIds: string[]): void;

  // Agent 任务
  startTask(type: AgentTask['type'], params: Record<string, unknown>): string; // 返回 taskId
  updateTask(taskId: string, updates: Partial<AgentTask>): void;
  completeTask(taskId: string, result: Record<string, unknown>): void;
  failTask(taskId: string, error: string): void;

  // 输入控制
  setInputText(text: string): void;
  setInputDisabled(disabled: boolean): void;

  // 快捷操作
  applyCodeDiff(messageId: string): Promise<void>;  // 应用消息中的代码变更到 timeline
  rollbackToVersion(versionIndex: number): Promise<void>; // 回滚到指定对话版本
}
```

**Agent 状态机：**

```
                    ┌─────────────┐
                    │    idle     │◄─────────────────┐
                    │ (等待输入)   │                  │
                    └──────┬──────┘                  │
                           │ sendMessage()           │
                           ▼                         │
                    ┌─────────────┐                  │
              ┌────►│  parsing    │                  │
              │     │ (意图解析)  │                  │
              │     └──────┬──────┘                  │
              │            │ 解析完成                 │
              │            ▼                         │
              │     ┌─────────────┐    失败           │
              │     │ generating  │─────────────────┤
              │     │ (代码生成)  │   failTask()      │
              │     └──────┬──────┘                  │
              │            │ 生成完成                 │
              │            ▼                         │
              └─────┌─────────────┐    用户取消        │
                    │ reviewing   │──────────────────┤
                    │ (等待确认)  │   cancelGeneration()
                    └─────────────┘
```

**跨 Store 影响：**
- `sendMessage()` → Agent 生成完成后 → `timelineStore` 应用变更
- `applyCodeDiff()` → 调用 `timelineStore.addTrack()` / `addClip()` 等
- `rollbackToVersion()` → 恢复对应版本的 `subproject.json` → `timelineStore.loadTimeline()`

---

### 4. uiStore

**职责**：界面布局、面板状态、选中元素、主题。

```typescript
// stores/uiStore.ts
interface UIState {
  // ── 布局 ──
  leftPanelWidth: number;            // 左侧面板宽度（px），默认 280
  rightPanelWidth: number;          // 右侧面板宽度（px），默认 320
  bottomPanelHeight: number;        // 底部时间线高度（px），默认 240
  isLeftPanelCollapsed: boolean;
  isRightPanelCollapsed: boolean;
  isBottomPanelCollapsed: boolean;

  // ── 面板内容 ──
  leftPanelTab: 'project' | 'asset' | 'preset';      // 左侧面板当前 Tab
  rightPanelMode: 'property' | 'quick-asset';        // 右侧面板模式
  isConversationPanelOpen: boolean;                  // 对话面板展开/收起
  conversationPanelHeight: number;                    // 对话面板高度（中间区域占比）

  // ── 弹窗/浮层 ──
  activeModal: ModalType | null;
  modalData: Record<string, unknown> | null;
  activeToast: ToastItem[];          // Toast 队列（最多 5 个）
  contextMenu: ContextMenuState | null;

  // ── 拖拽状态 ──
  dragState: DragState | null;      // 当前拖拽操作（用于跨组件拖拽反馈）

  // ── 主题 ──
  theme: 'dark' | 'light' | 'system';
  isReducedMotion: boolean;         // 是否开启减弱动画
}

interface UIActions {
  // 布局调整
  setLeftPanelWidth(width: number): void;
  setRightPanelWidth(width: number): void;
  setBottomPanelHeight(height: number): void;
  toggleLeftPanel(): void;
  toggleRightPanel(): void;
  toggleBottomPanel(): void;

  // Tab 切换
  setLeftPanelTab(tab: UIState['leftPanelTab']): void;
  setRightPanelMode(mode: UIState['rightPanelMode']): void;
  toggleConversationPanel(): void;
  setConversationPanelHeight(height: number): void;

  // 弹窗
  openModal(type: ModalType, data?: Record<string, unknown>): void;
  closeModal(): void;

  // Toast
  showToast(toast: Omit<ToastItem, 'id'>): string;  // 返回 toastId
  dismissToast(toastId: string): void;

  // 右键菜单
  openContextMenu(position: { x: number; y: number }, items: ContextMenuItem[]): void;
  closeContextMenu(): void;

  // 拖拽
  setDragState(state: DragState | null): void;

  // 主题
  setTheme(theme: UIState['theme']): void;
  setReducedMotion(enabled: boolean): void;
}

// 持久化到 settings.json 的字段
const UI_PERSISTENT_KEYS: (keyof UIState)[] = [
  'leftPanelWidth',
  'rightPanelWidth',
  'bottomPanelHeight',
  'isLeftPanelCollapsed',
  'isRightPanelCollapsed',
  'isBottomPanelCollapsed',
  'leftPanelTab',
  'theme',
  'isReducedMotion',
];
```

---

### 5. previewStore

**职责**：预览窗口状态、iframe 通信缓冲、性能指标。

```typescript
// stores/previewStore.ts
interface PreviewState {
  // ── 预览配置 ──
  scale: number;                     // 预览缩放 0.25-2.0，默认 1.0
  quality: 'full' | 'half' | 'quarter'; // 预览质量（降级策略）
  showSafeArea: boolean;             // 是否显示安全区域指示
  showGrid: boolean;                 // 是否显示辅助网格

  // ── iframe 状态 ──
  iframeReady: boolean;              // Remotion Player 是否就绪
  iframeUrl: string | null;          // 当前 iframe 加载的 URL
  lastReloadAt: number | null;       // 上次 reload 时间戳（防抖动）

  // ── 性能监控 ──
  fps: number;                       // 实时帧率（由 iframe 上报）
  frameDropCount: number;            // 丢帧计数（持续 3 秒低帧率触发降级）
  memoryUsageMB: number | null;      // 渲染进程内存占用（估算）

  // ── 全屏状态 ──
  isFullscreen: boolean;
}

interface PreviewActions {
  // 缩放与质量
  setScale(scale: number): void;
  setQuality(quality: PreviewState['quality']): void;
  toggleSafeArea(): void;
  toggleGrid(): void;

  // iframe 控制
  setIframeReady(ready: boolean): void;
  setIframeUrl(url: string): void;
  reloadIframe(): void;              // 强制 reload（Generator 代码更新后）

  // 性能
  updateFps(fps: number): void;
  resetFrameDropCount(): void;
  setMemoryUsage(mb: number): void;

  // 全屏
  enterFullscreen(): void;
  exitFullscreen(): void;
  toggleFullscreen(): void;
}
```

---

## 跨 Store 通信机制

### 1. 单向 Action 委托（推荐）

当一个 Store 的 Action 需要触发另一个 Store 的变更时，通过调用该 Store 的 Action，而非直接操作状态。

**示例：Agent 生成完成后更新 Timeline**

```typescript
// conversationStore.ts 中
applyCodeDiff: async (messageId) => {
  const diff = getMessageDiff(messageId);
  // 委托 timelineStore 执行变更
  useTimelineStore.getState().addTrack(diff.newTrack);
  useTimelineStore.getState().saveTimeline(); // 触发持久化
}
```

### 2. 订阅者模式（状态同步）

当一个 Store 的 State 变化需要被另一个 Store 监听时，使用 Zustand 的 `subscribe`：

**示例：projectStore.currentSubprojectId 变化时加载对话**

```typescript
// App.tsx 初始化时
useProjectStore.subscribe(
  (state) => state.currentSubprojectId,
  (subprojectId) => {
    if (subprojectId) {
      useConversationStore.getState().loadConversation(subprojectId);
      useTimelineStore.getState().loadTimeline(subprojectId);
    }
  }
);
```

### 3. 禁止的做法

| ❌ 禁止 | ✅ 正确 |
|---------|---------|
| `useTimelineStore.getState().timeline = newTimeline` | `useTimelineStore.getState().addTrack(track)` |
| Store A 直接修改 Store B 的 State | Store A 调用 Store B 的 Action |
| 在 Action 内部调用 `getState()` 获取其他 Store 做条件判断 | 通过订阅分离关注点 |

---

## 持久化策略

### 1. projectStore 持久化

**触发条件：**
- `createProject()` / `renameProject()` / `deleteProject()` → 立即同步 `library.db`
- `openProject()` → 更新 `last_opened_at`

**方式：** 通过 IPC `main:updateProjectIndex` → 主进程写 SQLite。

### 2. timelineStore 持久化

**触发条件：**
- 防抖 500ms：任何修改 `timeline` 的操作后 500ms 无新操作 → 触发保存
- 关键操作立即保存：删除子项目、重命名、切换子项目前
- 用户手动保存（`Ctrl+S`）→ 立即保存

**方式：**
```
timelineStore.saveTimeline()
  → IPC invoke('main:saveSubproject', { subprojectId, timelineJSON })
  → 主进程校验 JSON → 写入 subproject.json
  → 成功后 timelineStore.hasUnsavedChanges = false
```

### 3. conversationStore 持久化

**触发条件：** 每收到一条 Assistant 完整消息后自动保存（流式输出结束后）。

**方式：** 通过 IPC `main:saveConversation` → 写入 `conversation.json`。

### 4. uiStore 持久化

**触发条件：** 面板尺寸/折叠状态变化后防抖 1 秒。

**方式：** 通过 IPC `main:saveSettings` → 写入 `settings.json` 的 `ui` 字段。

---

## Store 与 Remotion iframe 的通信

```
┌─────────────────────────────────────────────────────────────┐
│                    Electron Renderer                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │ timelineStore│  │ previewStore │  │    UI Components    │  │
│  │ (播放控制)    │  │ (iframe 状态)│  │ (Player 容器)       │  │
│  └──────┬───────┘  └──────┬──────┘  └──────────┬──────────┘  │
│         │                 │                     │              │
│         └─────────────────┴─────────────────────┘              │
│                           │                                   │
│                    window.postMessage                          │
│                           │                                   │
└───────────────────────────┼───────────────────────────────────┘
                            │
                    ┌───────▼────────┐
                    │  iframe (Remotion│
                    │   Player)      │
                    │  - seekTo()    │
                    │  - play()      │
                    │  - pause()     │
                    └───────┬────────┘
                            │
                    window.parent.postMessage
                            │
                    ┌───────▼────────┐
                    │  帧变更事件     │
                    │  FRAME_CHANGE  │
                    └────────────────┘
```

**通信协议（iframe ↔ Renderer）：**

| 方向 | 消息类型 |  Payload | 说明 |
|------|----------|----------|------|
| Renderer → iframe | `PLAY` | - | 播放 |
| Renderer → iframe | `PAUSE` | - | 暂停 |
| Renderer → iframe | `SEEK` | `{ frame: number }` | 跳转到指定帧 |
| Renderer → iframe | `SET_SCALE` | `{ scale: number }` | 设置缩放 |
| iframe → Renderer | `FRAME_CHANGE` | `{ frame: number }` | 播放头位置变化 |
| iframe → Renderer | `READY` | - | Player 加载完成 |
| iframe → Renderer | `ERROR` | `{ message: string }` | 渲染错误 |
| iframe → Renderer | `FPS_REPORT` | `{ fps: number }` | 实时帧率上报 |

---

## 性能优化规则

1. **批量更新**：Generator 生成多个轨道时，使用单个 `setState` 批量写入，而非逐条 `addTrack`。
2. **选择器订阅**：组件只订阅需要的字段，避免无关状态变更触发重渲染。
   ```typescript
   // ✅ 只订阅 currentFrame，不订阅整个 timeline
   const currentFrame = useTimelineStore((state) => state.currentFrame);
   ```
3. **防抖持久化**：写盘操作统一防抖，避免频繁 IO。
4. **历史栈截断**：`past[]` 超过 50 步时，自动丢弃最早的记录。

---

## 调试与开发工具

- 使用 Zustand DevTools（通过 Redux DevTools Extension）监控所有 Store 状态变化。
- 每个 Action 执行时打印日志（开发模式）：`[StoreName] actionName(payload)`
- 历史栈支持导出为 JSON（调试菜单 → "导出时间线历史"）。
