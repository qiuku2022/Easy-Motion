# 04 — 文件清单与 PR 拆分

## 新建文件

```
apps/electron/src/main/agent/
├── remotion-sandbox.js          # 路径白名单、normalize
├── remotion-context.js          # 读写、snapshot、changeLog
├── remotion-compile-check.js    # tsc / 超时
├── remotion-registry.js         # 更新 custom-registry.ts 的 AST/模板逻辑
├── prompts/
│   └── remotion-code.js         # Remotion 代码 system prompt 段
└── tools/
    └── remotion-code.js         # 6 个 LangChain tools

apps/electron/scripts/
├── test-remotion-sandbox.js
├── test-remotion-code-tools.js
├── test-remotion-code-agent-eval.js
└── test-remotion-code-agent-live.js   # 需 API Key

apps/electron/resources/templates/default-project/subprojects/default/remotion/src/
├── components/custom/
│   └── .gitkeep
└── presets/
    └── custom-registry.ts       # 空 map 模板

docs/agent-eval/
└── remotion-code-tasks.json     # Phase 3
```

## 修改文件

| 文件 | 变更 |
|------|------|
| `agent/graph.js` | 挂载 remotion tools；export `createHybridAgent` |
| `agent/index.js` | RemotionContext；remotionChanged；compile 失败回滚 |
| `agent/timeline-context.js` 或新建 | `formatChangeSummary` 合并 remotion changeLog |
| `services/agent-service.js` | complete payload 增加 remotionCodeUpdated |
| `services/preview-service.js` | `ensureCustomComponentSupport` |
| `services/timeline-service.js` | drift 排除 custom 路径 |
| `services/remotion-project.js` | `ensureCustomRegistry` 创建目录 |
| `package.json` | `test:m5.2` script |
| `AGENTS.md` | 链接 M5.2 计划（一行） |

渲染进程（Phase 2 小改）：

| 文件 | 变更 |
|------|------|
| `types/easyMotion.ts` | complete payload 类型 |
| `stores/conversationStore.ts` | 处理 remotionCodeUpdated |

模板（仅新创建项目自带；旧项目靠 migrate）：

| 文件 | 变更 |
|------|------|
| `.../MainSequence.tsx` | fallback resolveCustomComponent |
| `.../presets/custom-registry.ts` | 新建 |

## PR 拆分建议

| PR | 标题 | 范围 | 可合并条件 |
|----|------|------|------------|
| **PR-1** | feat(agent): remotion sandbox + read/write tools | Phase 1 1.1–1.8 | `test:m5.2` 沙箱部分绿 |
| **PR-2** | feat(agent): custom registry + registerCustomComponent | Phase 2 2.1–2.4 | eval 写文件 + registry 绿 |
| **PR-3** | feat(agent): preview reload + drift + UI payload | Phase 2 2.5–2.7 | 手测预览 + `test:m5` 仍绿 |
| **PR-4** | docs: agent-eval remotion-code + LLM-Agent设计 | Phase 3 | eval 脚本绿 |

每 PR 保持 `pnpm test`（m1–m5 + agent-eval）不回退。

## npm 依赖

M5.2 **预计无新 npm 依赖**（tsc 用用户 remotion 项目内 `node_modules/typescript` 或 remotion 自带；若无则 compile check 降级为 esbuild 语法扫描 + security-scan only，文档标注）。

可选 devDependency（若需在 CI 测 compile）：

- 无 — 单测用 fixture remotion mini 项目目录

## package.json scripts（规划）

```json
{
  "test:m5.2": "node scripts/test-remotion-sandbox.js && node scripts/test-remotion-code-tools.js && node scripts/test-remotion-code-agent-eval.js",
  "test": "pnpm test:m1 && pnpm test:m2 && pnpm test:m3 && pnpm test:m5 && pnpm test:m5.2"
}
```
