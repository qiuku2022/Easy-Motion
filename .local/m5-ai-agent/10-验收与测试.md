# 10 — 验收与测试

## M5 完成定义（Done）

- [ ] 验收 A1–A5 全部通过
- [ ] A6 通过或明确标为 M5.1 stretch 并排期
- [ ] IPC 对齐 `conversation` + `llm` + `settings` 域
- [ ] `conversationStore` 对齐状态管理设计
- [ ] 无 `.env` 明文 Key 在版本库
- [ ] 根 README 更新为 M5 完成

---

## 验收标准对照

| ID | 场景 | 步骤 | 通过标准 |
|----|------|------|----------|
| A1 | 创建文字 | 输入「创建一个标题写着 Hello」 | text 轨道 + 预览显示 Hello |
| A2 | 微调 | 选中文字，「字体大一点」 | fontSize 增，预览变大 |
| A3 | 持久化 | 发消息 → 关项目 → 重开；切换子项目 | 历史保留且隔离 |
| A4 | 降级 | 模拟 LLM 超时两次 | 简化模式生成基础文字 |
| A5 | 安全 | 注入含 `fs` 的生成请求 | 拒绝写入 + 提示 |
| A6 | 多模态 | 上传参考图 + 布局描述 | 布局大致相似 |

---

## 测试分层

### 单元测试

路径：`apps/electron/src/main/agent/__tests__/`

| 模块 | 用例 |
|------|------|
| `timeline-tools` | createTrack、createClip、updateClip 嵌套路径、queryElement |
| `fallback-templates` | 关键词触发 Hello 模板 |
| `prompts` | 上下文拼装含 fps/选中 clip |

运行：`pnpm test`（或项目现有 test 命令，需在 PR-4 接入）

### 集成测试

- mock `llm-service` 返回固定 tool_calls → 验证 timeline JSON 落盘
- mock 流式 chunk → 验证 `conversation-service` save/load 往返

### 手动 E2E（发布前必跑）

1. 新建项目 → 配 Key → AI Tab 发消息
2. A1 脚本
3. A2 脚本
4. 切换子项目验证 A3
5. 断网验证 A4 错误 UI
6. （可选）A6 参考图

---

## UI 回归

- [ ] 切换右侧 Tab，对话 state 不丢
- [ ] Agent 执行中时间线仍可只读浏览（不崩溃）
- [ ] 预览在 AI 修改后自动刷新（≤2s）
- [ ] 工具栏「AI 助手」按钮跳转 Tab 正常

---

## Prompt 回归集（10 条）

维护在 `apps/electron/src/main/agent/__fixtures__/prompt-regression.json`（Phase 4 起）：

1. 创建一个标题写着 Hello
2. 字体大一点
3. 颜色改成蓝色
4. 加一个淡入效果
5. 把标题移到中间
6. 删除这个文字
7. 创建副标题写着 World
8. 动画快一点
9. 背景改成黑色（若有 shape/animation track）
10. 标题小一点

每条记录：`input`、`expectedTools` 或 `expectedClipProps` 快照。

---

## 性能基线（非阻断）

| 指标 | 目标 |
|------|------|
| 首 token 延迟 | < 5s（正常网络） |
| A1 端到端 | < 30s（含 generate + HMR） |
| conversation save | < 200ms |

---

## 发布检查清单

- [ ] `.env.example` 已更新，`.env` 在 gitignore
- [ ] 错误码与 IPC 规范一致
- [ ] 对话日志无 API Key 泄露
- [ ] CI lint + test 绿
