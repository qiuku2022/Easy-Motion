# 11 — shadcn/ui 迁移完成记录

> 更新：2026-06-11 · **v1 迁移已收尾**

---

## 阶段状态

| 阶段 | 内容 | 状态 |
|------|------|------|
| 0 | init + Tailwind 语义色桥接 | ✅ |
| 1 | Cinema Dark / Action Red 主题 token | ✅ |
| 2 | 基础 primitive（Button/Input/Tooltip 等） | ✅ |
| 3 | 浮层（Tabs/Dropdown/ContextMenu/Dialog） | ✅ |
| 4 | ScrollArea / Sonner / PanelResizer 语义色 | ✅ |
| 5 | Form + Select/Slider/Switch/Checkbox | ✅ |
| 6 | 时间线边界 + TrackHeader 控件 | ✅ |
| 7 | 侧栏/预览剩余 primitive + 依赖清理 | ✅ |
| 8 | 颜色拾色器 + 缩放控件 + 壳层语义色 | ✅ |
| 9 | 文档同步 + 自动化验收 | ✅ |
| — | 手动 UI 回归 | ⏳ 建议 `pnpm dev` 过一遍 `09-验收与回归.md` |

---

## 阶段 9 变更摘要

| 项 | 内容 |
|----|------|
| `TrackHeader` / `ClipFloatingToolbar` | 剩余 `em-*` → 语义 token |
| `MASTER.md` | shadcn 语义类与 `em-*` 别名对照表 |
| `README.md` / `开发者README.md` | shadcn 路径与 `npx shadcn add` 说明 |
| 自动化 | `pnpm build:renderer`、`pnpm lint` 通过 |

---

## 已安装 shadcn 组件

`alert-dialog`, `button`, `checkbox`, `context-menu`, `dialog`, `dropdown-menu`, `form`, `input`, `label`, `popover`, `resizable`, `scroll-area`, `select`, `separator`, `slider`, `sonner`, `switch`, `tabs`, `textarea`, `tooltip`

---

## 刻意保留（非遗漏）

| 项 | 原因 |
|----|------|
| `TimelineBody`, `ClipBlock`, `Playhead`, `TimelineRuler` 等 | 性能与帧精度 |
| 自定义 `PanelResizer` | 与 uiStore 像素宽度 + 折叠逻辑契合；`resizable` 已安装备用 |
| 画布内部分 `em-*` | 与语义 token 等价，可渐进替换 |

---

## 后续迭代（非阻塞）

- `ResizablePanelGroup` 替换 `PanelResizer`
- 提取 `packages/ui` 共享包
- 新功能（`BottomConversationBar` 等）直接用 shadcn 开发
