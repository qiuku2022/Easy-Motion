# 08 — 实施清单（确认方案后执行）

## Phase 1 — Token（约 15 分钟）

- [ ] 用 `03` 或 `04` 替换 `apps/electron/src/renderer/src/index.css` 的 `:root/.dark` 块
- [ ] 方案 B：在 `tailwind.config.js` `extend.colors` 增加 `timeline.*`
- [ ] `pnpm build:renderer` 通过

## Phase 2 — 按钮用法（约 30 分钟）

- [ ] `TopToolbar.tsx` — 保存等改 `outline`（若有 default）
- [ ] `ProjectPanel.tsx` — 新建 `default` → `outline` 或 `secondary`
- [ ] `RightPanel.tsx` — AI 发送保持 `default` icon 即可
- [ ] `TimelinePanel.tsx` — 横幅按钮已为 outline，检查即可
- [ ] grep 全项目 `variant="default"`，逐项判断

## Phase 3 — 圆角清理（约 20 分钟）

- [ ] 删除 Button 上无必要的 `rounded-sm`
- [ ] 保留时间线 `ClipFloatingToolbar` 等极小控件例外（可选）

## Phase 4 — 时间线（仅方案 B）

- [ ] 按 `06-时间线语义色.md` 改 5 个画布组件
- [ ] 播放头无 CSS transition（保持）

## Phase 5 — 文档

- [ ] 更新 `docs/design-system/easymotion/MASTER.md` 配色表
- [ ] 更新 `UI布局与交互设计-优化版.md` §0.1–0.4
- [ ] 更新 `组件库清单.md` 如有必要

## 验收

- [ ] 顶栏无大面积红色按钮
- [ ] 壳层背景无紫调（方案 A）
- [ ] 输入聚焦为灰色环，非青色
- [ ] 按钮圆角与输入框视觉层级一致
- [ ] `pnpm dev` 目视 10 分钟无刺眼对比

---

## 旧 vs 新 token 对照（方案 A）

| 角色 | 旧（Cinema） | 新（Neutral） |
|------|-------------|---------------|
| background | #0F0F23 紫黑 | ~#0a0a0a 纯灰黑 |
| primary 按钮 | #E11D48 红 | ~#e5e5e5 浅灰白 |
| ring | #0D9488 青 | ~#737373 灰 |
| border | #2D2D4A 实色 | white 10% alpha |
