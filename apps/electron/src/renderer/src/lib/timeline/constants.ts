/** 轨道头固定宽度（与 UI 文档 §3.1 一致） */
export const TRACK_HEADER_WIDTH = 140;

/** 时间线标尺高度 */
export const RULER_HEIGHT = 28;

/** 单行轨道内容高度 */
export const TRACK_ROW_HEIGHT = 36;

/** 片段块上下内边距（与 ClipBlock inset 一致） */
export const TRACK_CLIP_INSET_Y = 4;
